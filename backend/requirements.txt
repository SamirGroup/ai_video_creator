# --- Core framework ---
Django==5.2.17
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1

# --- Database ---
psycopg[binary]==3.2.13

# --- Config / env ---
django-environ==0.14.0

# --- Async tasks / scheduling ---
celery==5.6.3
redis==7.4.0
django-celery-beat==2.9.0

# --- Outbound HTTP (OpenRouter / OpenAI Moderation provider calls) ---
# Pinned explicitly rather than relying on it arriving transitively via stripe /
# google-api-python-client, since video_pipeline imports it directly.
requests==2.34.2

# --- API / CORS / OpenAPI ---
django-cors-headers==4.9.0
django-filter==25.2
drf-spectacular==0.30.0

# --- Security / crypto ---
cryptography==46.0.6

# --- Storage / media ---
boto3==1.43.6
django-storages==1.14.6

# --- Payments ---
stripe==15.0.1

# --- Google / YouTube / AdSense OAuth ---
google-api-python-client==2.200.0
google-auth-oauthlib==1.4.1
google-auth==2.41.1

# --- Observability ---
sentry-sdk==2.68.1

# --- WSGI server (Linux/prod) ---
gunicorn==26.2.0

# --- Testing ---
pytest==8.4.2
pytest-django==4.14.0
factory-boy==3.3.3
